from setuptools import setup, find_packages

setup(
    name='Vanguard',
    version='1.0',
    description='Tool analyzer and generation of reports',
    author='LaymsS',
    author_email='',
    packages=find_packages(),
    install_requires=[

    ]
)